---
- name: set front page
  command: 'drush vset site_frontpage islandora-collection-landing-pages'
  args:
    chdir: "{{ drupal_core_path }}"
    
- name: set theme
  command: 'drush vset theme_default subtheme_lsu'
  args:
    chdir: "{{ drupal_core_path }}"
    
- name: set site name
  command: 'drush vset site_name {{ drupal_site_name  }}'
  args:
    chdir: "{{ drupal_core_path }}"

- name: limit solr fields
  command: 'drush vset islandroa_solr_limit_result_fields 1'
  args:
    chdir: "{{ drupal_core_path }}"

- name: set metadata display
  command: 'drush vset islandora_metadata_display islandora_solr_metadata'
  args: 
    chdir: "{{ drupal_core_path }}"

#- name: revert block setting
#  command: 'drush features-revert blocks_lsu'
#  args:
#    chdir: "{{ drupal_core_path }}"
# this command features-revert (which works fine in cli once provisioned) doesn't work here. drush can't find features-revert command

