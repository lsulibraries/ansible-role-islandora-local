---
local_features: 
    - solr_lsu_settings
    - metadata_display_lsu
    - collection_landing_page
    - islandora_collection_landing_pages
    - blocks_lsu
    - islandora_mods_display_changes

local_mods:
    - islandora_mods_display

themes_dir: "{{ drupal_core_path }}/sites/all/themes"

local_subtheme:
    - subtheme_lsu


drupal_site_name: 'LDL\ Islandora'
fedora_namespace: "default_namespace"

collections_file: collection_data.tsv
cdm_create_collections: true
